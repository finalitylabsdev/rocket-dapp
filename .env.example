# ──────────────────────────────────────────────────────────────────────────────
# Entropy — Environment Variables
# Copy to .env and fill in project-specific values before running.
# ──────────────────────────────────────────────────────────────────────────────

# ── Supabase ──────────────────────────────────────────────────────────────────
# Required. Project URL and publishable (anon) key from the Supabase dashboard.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Required only for SIM_WALLET_SOURCE=supabase
# SUPABASE_SERVICE_ROLE_KEY=

# ── SIWE (Sign-In with Ethereum) ──────────────────────────────────────────────
# Required. Must match the domain the app is served from.
VITE_SIWE_URI=https://example.com/
VITE_SIWE_DOMAIN=example.com

# ── ETH Lock ──────────────────────────────────────────────────────────────────
# Recipient address for ETH lock transactions. Leave blank to use the default.
# VITE_ETH_LOCK_RECIPIENT=0x...

# ── Economy Tuning ────────────────────────────────────────────────────────────
# PRODUCTION defaults are applied automatically when DEV_FAST_ECONOMY is false.
# Only override individual values if you need non-standard production tuning.
VITE_SPEC_DEV_FAST_ECONOMY=false
# VITE_SPEC_WHITELIST_BONUS_FLUX=0       (default when DEV_FAST_ECONOMY=false)
# VITE_SPEC_DAILY_CLAIM_FLUX=1           (default when DEV_FAST_ECONOMY=false)
# VITE_SPEC_FAUCET_INTERVAL_SECONDS=86400 (default when DEV_FAST_ECONOMY=false)
# VITE_SPEC_BOX_PRICE_MULTIPLIER=1
# VITE_SPEC_AUCTION_MAX_BID_FLUX=100000

# ── Feature Flags ─────────────────────────────────────────────────────────────
# All default to true. Set to "false" to disable a feature during staged rollout.
# VITE_FLAG_STAR_VAULT_ENABLED=true
# VITE_FLAG_NEBULA_BIDS_ENABLED=true
# VITE_FLAG_ROCKET_LAB_ENABLED=true
# VITE_FLAG_DEX_ENABLED=true
# VITE_FLAG_FAUCET_ENABLED=true
# VITE_FLAG_AUCTION_SCHEDULER_ENABLED=true

# ── Synthetic Traffic Simulator ───────────────────────────────────────────────
# Default mode uses private keys from .env.
# SIM_WALLET_SOURCE=env
# SIM_WALLET_PRIVATE_KEYS=0x...,0x...

# Managed wallet mode stores encrypted simulator keys in Supabase.
# SIM_WALLET_SOURCE=supabase
# SIM_SUPABASE_SERVICE_ROLE_KEY=        (falls back to SUPABASE_SERVICE_ROLE_KEY)
# SIM_MANAGED_WALLETS_ENCRYPTION_KEY=   (64 hex chars or base64 for 32 bytes)
# SIM_MANAGED_WALLET_TARGET_COUNT=5
# SIM_IMPORT_ENV_WALLETS_TO_SUPABASE=true
# SIM_AUTO_CONFIRM_ETH_LOCK=true
# SIM_BOOTSTRAP_ONLY=false
# SIM_MANAGED_WALLET_KID=sim-local-v1
# SIM_MANAGED_WALLET_GENERATION_MIN_INTERVAL_MS=120000
# SIM_MANAGED_WALLET_GENERATION_MAX_INTERVAL_MS=420000

# Optional pseudo ETH lock controls for managed synthetic wallets.
# SIM_ETH_LOCK_TO_ADDRESS=0x...
# SIM_ETH_LOCK_AMOUNT_WEI=1000000000000000
